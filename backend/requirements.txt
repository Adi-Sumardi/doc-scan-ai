# 🚀 DOC SCAN AI - PRODUCTION REQUIREMENTS
# Professional document scanning with 99%+ OCR accuracy

# ===== CORE FASTAPI SERVER =====
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
aiofiles==24.1.0
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# ===== DATABASE & STORAGE =====
sqlalchemy==2.0.36
pymysql==1.1.1
alembic==1.14.0
mysql-connector-python==9.1.0

# ===== SECURITY & VALIDATION =====
python-magic==0.4.27
python-dotenv==1.0.1
aioredis==2.0.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
slowapi==0.1.9              # Rate limiting for authentication endpoints 

# ===== MONITORING & LOGGING =====
prometheus-client==0.21.1
structlog==24.4.0

# ===== OCR ENGINES =====
surya-ocr>=0.6.0            # Free local OCR (primary engine) - Surya OCR
nest-asyncio==1.6.0         # For running asyncio loops inside other loops (gRPC compatibility)

# ===== IMAGE PROCESSING (MINIMAL) =====
# REMOVED: opencv-python, opencv-contrib-python, scikit-image, albumentations
# Google Document AI handles image preprocessing
pillow==10.1.0              # Basic image handling only

# ===== PDF PROCESSING =====
PyPDF2==3.0.1
pdf2image==1.17.0
pdfplumber==0.11.7
pdfminer.six==20250506
pymupdf==1.24.5             # Faster PDF processing
pypdfium2==4.30.0
# unstructured[pdf]==0.15.0   # Commented out, can be heavy

# ===== AI MODELS REMOVED =====
# REMOVED: transformers, torch, torchvision, sentence-transformers
# REMOVED: scikit-learn, xgboost, lightgbm
# REMOVED: onnx, onnxruntime
# Using only Google Document AI + LLM APIs (OpenAI/Anthropic) now

# Scientific Computing
numpy==1.26.2
scipy==1.11.4
pandas==2.1.3
matplotlib==3.8.2
seaborn==0.13.2

# Image Processing (Minimal)
# REMOVED: imageio, tifffile, opencv-python-headless
# Google Document AI handles all image processing

# Export Libraries
openpyxl==3.1.5
reportlab==4.4.3

# Core Dependencies
PyYAML==6.0.2
requests==2.32.5
tqdm==4.67.1
joblib==1.5.2
threadpoolctl==3.6.0

# OCR-specific Dependencies REMOVED
# REMOVED: python-bidi, pyclipper, imutils
# No longer needed without local OCR engines

# Cryptography and Security
cryptography==46.0.1
cffi==2.0.0
pycryptodome==3.23.0

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2
charset-normalizer==3.3.2
urllib3==2.2.3    # ✅ FIX: Upgraded to latest - google-auth now supports 2.x (security fixes)
certifi==2025.8.3
regex==2025.9.1

# ===== CLOUD AI SERVICES (CORE) =====
# Google Document AI - Primary and ONLY OCR engine
google-cloud-documentai==2.26.0      
google-cloud-storage==2.10.0         
google-auth==2.23.0                   

# Smart Mapper LLM clients
openai==1.50.2                        # For GPT-based field extraction
anthropic==0.71.0                     # For Claude-based field extraction (upgraded to fix proxy issue)

# REMOVED: Azure Cognitive Services, AWS Textract
# Using only Google Document AI + Smart Mapper GPT/Claude now

# ===== PRODUCTION DEPLOYMENT =====
# Uncomment for production:
# gunicorn==21.2.0           # Production WSGI server
# supervisor==4.2.5          # Process management
psutil==7.1.3
